import { DashboardSkeleton } from "@/components/ui/skeletons";

export default function Loading() {
  // DashboardSkeleton ya está diseñado en tus componentes UI para imitar
  // exactamente la estructura de KPIs + Gráfico + Tabla.
  return <DashboardSkeleton />;
}
